import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#f7f9fc',
    padding: 16,
  },

  bigText: {
    fontSize: 30, 
    fontWeight: "bold",
    color: "#000",
  },
});
